import type { PortalDefinition } from "@/portals/types";

export const adminPortal: PortalDefinition = {
  title: "Union admin portal",
  description: "Manage association members and registrations.",
  tabs: [],
};
